_schema-version: "3.2"
ID: tankreporting
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: tankreporting-destination-content
  type: com.sap.application.content
  requires:
  - name: tankreporting-destination-service
    parameters:
      content-target: true
  - name: tankreporting-repo-host
    parameters:
      service-key:
        name: tankreporting-repo-host-key
  - name: tankreporting-uaa
    parameters:
      service-key:
        name: tankreporting-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tankreporting_html_repo_host
          ServiceInstanceName: tankreporting-html5-srv
          ServiceKeyName: tankreporting-repo-host-key
          sap.cloud.service: tankreporting
        - Authentication: OAuth2UserTokenExchange
          Name: tankreporting_uaa
          ServiceInstanceName: tankreporting-xsuaa-srv
          ServiceKeyName: tankreporting-uaa-key
          sap.cloud.service: tankreporting
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: tankreporting-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tankreporting-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tankreporting.zip
      name: tankreporting
      target-path: resources/
- name: tankreporting
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tankreporting-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tankreporting-destination-service
    service-plan: lite
- name: tankreporting-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tankreporting-xsuaa-srv
    service-plan: application
- name: tankreporting-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tankreporting-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
